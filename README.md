# WebOS

First-pass WebOS-style dashboard UI starter.

## Run locally

Open `/home/runner/work/WebOS/WebOS/index.html` directly in a browser, or serve the folder:

```bash
cd /home/runner/work/WebOS/WebOS
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
